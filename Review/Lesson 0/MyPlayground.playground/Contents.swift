import UIKit

var str = "Hello, playground"

// data is stored in memory
// to work, we need to reference memories/data
var firstName /*this declares firstName as a variable*/ = "Tom" /*assign the string data "Tom" to firstName*/

print(firstName) // prints the data stored in firstName

var stockPrice = 100
print(stockPrice)

stockPrice = 50 // here, since stockPrice is already declared, no need to redeclare it - this is assigning the new value 50

// stockPrice = "Tom" // you cannot assign a different data type to one that is expecting a value type

let lastName = "Smith" // this is a constant - constants cannot reassign different data


